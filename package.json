{
  "name": "laravel-vue-helpers",
  "version": "0.0.3",
  "description": "Vue helpers for a laravel application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preupload": "git add . && git commit",
    "upload": "npm version patch",
    "postupload": "git push -u --all && git push -u --tags && npm publish"
  },
  "author": "",
  "license": "ISC"
}
